module.exports = {
    theme: {
      extend: {
        animation: {
          'spin-slow': 'spin 20s linear infinite',
          'pulse': 'pulse 1s cubic-bezier(0.4, 0, 0.6, 1) infinite',
        },
      },
    },
  }